version: v1
http:
  host: "0.0.0.0"
  port: 8080
database:
  uri_resolver: env://DATABASE_CONNECTION
authentication:
  jwt:
    algorithm: HS256
    signing_key_resolver: env://JWT_SECRET
    verifying_key_resolver: env://JWT_SECRET
    lifetime: 1d
  refresh_token_lifetime: 1d
